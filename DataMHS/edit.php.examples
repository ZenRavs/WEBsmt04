<?php
    session_start();
    if (isset($_SESSION['user']['userid'])) {
        $id_edit = $_POST['userid'];
        echo $id_edit;
    }
    else
    {
        echo 'Session doesnt start yet! <a href="main.php">Back</a>';
    }